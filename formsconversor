<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>FiberWan – Solicitação de Orçamento</title>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Sora:wght@400;600;700&display=swap');

    :root {
        --fw-vermelho: #e70404;
        --fw-vermelho-hover: #c90303;
        --fw-preto-card: #0a0a0a;
        --fw-borda: #222222;
        --fw-branco: #ffffff;
        --fw-cinza-texto: #cccccc;
        --fw-input-bg: #f4f5f7;
        --font-main: 'Sora', sans-serif;
    }

    .tf-card {
        font-family: var(--font-main);
        background: var(--fw-preto-card);
        border: 1px solid var(--fw-borda);
        border-radius: 12px;
        padding: 50px 40px;
        box-shadow: 0 20px 40px rgba(0,0,0,0.6);
        max-width: 550px;
        margin: 0 auto;
        color: var(--fw-branco);
        text-align: left;
    }

    .tf-sup-title {
        color: var(--fw-vermelho);
        font-size: 0.85rem;
        font-weight: 800;
        letter-spacing: 1px;
        text-transform: uppercase;
        margin-bottom: 10px;
    }

    .tf-main-title {
        font-size: 1.8rem;
        font-weight: 700;
        margin-bottom: 25px;
        line-height: 1.3;
    }

    .tf-progress-container {
        display: flex;
        gap: 8px;
        margin-bottom: 35px;
    }

    .tf-progress-bar {
        height: 5px;
        flex: 1;
        background: #333333;
        border-radius: 3px;
        transition: background 0.4s ease;
    }

    .tf-progress-bar.active {
        background: var(--fw-vermelho);
    }

    .tf-step-title {
        font-size: 1.3rem;
        font-weight: 700;
        margin-bottom: 25px;
        color: var(--fw-branco);
    }

    .tf-group {
        margin-bottom: 24px;
    }

    .tf-group label {
        display: block;
        font-size: 1.05rem;
        font-weight: 600;
        margin-bottom: 10px;
        color: var(--fw-branco);
    }

    .tf-group input, .tf-group select {
        width: 100%;
        padding: 18px;
        background: var(--fw-input-bg);
        border: 2px solid transparent;
        border-radius: 8px;
        color: #000000;
        font-family: var(--font-main);
        font-size: 1.2rem;
        font-weight: 600;
        outline: none;
        transition: all 0.3s ease;
        appearance: none;
        box-sizing: border-box;
    }

    .tf-group input::placeholder {
        font-weight: 400;
        color: #7a7a7a;
        font-size: 1.05rem;
    }

    .tf-group input:focus, .tf-group select:focus {
        border-color: var(--fw-vermelho);
        background: var(--fw-branco);
    }

    .tf-step {
        display: none;
        animation: fadeIn 0.4s ease;
    }

    .tf-step.active {
        display: block;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .tf-footer {
        display: flex;
        gap: 15px;
        margin-top: 35px;
    }

    .btn-tf {
        flex: 1;
        padding: 20px 16px;
        font-size: 1.2rem;
        font-weight: 700;
        border-radius: 8px;
        cursor: pointer;
        font-family: var(--font-main);
        transition: all 0.3s ease;
        text-align: center;
        border: none;
    }

    .btn-primary {
        background: var(--fw-vermelho);
        color: var(--fw-branco);
    }

    .btn-primary:hover:not(:disabled) {
        background: var(--fw-vermelho-hover);
        transform: translateY(-2px);
    }

    .btn-primary:disabled {
        opacity: 0.7;
        cursor: not-allowed;
    }

    .btn-secondary {
        background: #1a1a1a;
        color: var(--fw-cinza-texto);
        border: 1px solid #333333;
    }

    .btn-secondary:hover {
        background: #252525;
        color: var(--fw-branco);
    }

    .tf-error {
        color: var(--fw-vermelho);
        font-size: 0.9rem;
        margin-top: 5px;
        display: none;
        font-weight: 600;
    }

    /* Tela de Sucesso */
    .tf-success {
        display: none;
        text-align: center;
        padding: 20px 0;
        animation: fadeIn 0.5s ease;
    }

    .tf-success-icon {
        width: 64px;
        height: 64px;
        background: var(--fw-vermelho);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 24px;
        font-size: 2rem;
    }

    .tf-success h3 {
        font-size: 1.6rem;
        font-weight: 700;
        margin-bottom: 12px;
        color: var(--fw-branco);
    }

    .tf-success p {
        color: var(--fw-cinza-texto);
        font-size: 1rem;
        line-height: 1.6;
    }

    @media (max-width: 768px) {
        .tf-card {
            padding: 30px 20px;
            max-width: 100%;
        }

        .tf-main-title { font-size: 1.5rem; }
        .tf-step-title { font-size: 1.2rem; }
        .tf-group label { font-size: 1rem; }
        .tf-group input, .tf-group select { font-size: 1.1rem; padding: 15px; }
        .btn-tf { padding: 16px; font-size: 1.1rem; }
    }
</style>
</head>
<body>

<div class="tf-card" id="formulario">
    <div class="tf-sup-title">Solicitação de Orçamento</div>
    <div class="tf-main-title">Fale com um especialista FiberWan</div>

    <div class="tf-progress-container">
        <div class="tf-progress-bar active" id="bar-1"></div>
        <div class="tf-progress-bar" id="bar-2"></div>
    </div>

    <form id="tf-lead-form" novalidate>

        <!-- ETAPA 1 -->
        <div class="tf-step active" id="step-1">
            <div class="tf-step-title">Dados de contato</div>

            <div class="tf-group">
                <label for="tf-nome">Nome Completo</label>
                <input type="text" id="tf-nome" name="nome" placeholder="Seu nome" required>
            </div>

            <div class="tf-group">
                <label for="tf-email">E-mail Profissional</label>
                <input type="email" id="tf-email" name="email" placeholder="email@empresa.com.br" required>
            </div>

            <div class="tf-group">
                <label for="tf-telefone">Telefone / WhatsApp</label>
                <input type="text" inputmode="numeric" id="tf-telefone" name="telefone" placeholder="(11) 90000-0000" maxlength="15" autocomplete="off" required>
            </div>

            <div class="tf-error" id="error-step-1">Preencha todos os campos corretamente para avançar.</div>

            <div class="tf-footer">
                <button type="button" class="btn-tf btn-primary" onclick="nextStep()">Continuar</button>
            </div>
        </div>

        <!-- ETAPA 2 -->
        <div class="tf-step" id="step-2">
            <div class="tf-step-title">Dados da empresa</div>

            <div class="tf-group">
                <label for="tf-empresa">Nome da Empresa</label>
                <input type="text" id="tf-empresa" name="empresa" placeholder="Sua empresa" required>
            </div>

            <div class="tf-group">
                <label for="tf-segmento">Segmento de atuação</label>
                <select id="tf-segmento" name="segmento" required>
                    <option value="" disabled selected>Selecione um segmento</option>
                    <option>Provedor de internet / ISP</option>
                    <option>Integrador de redes ou segurança</option>
                    <option>Empresa de telecomunicações</option>
                    <option>Distribuidor de tecnologia</option>
                    <option>Instalador autônomo / técnico</option>
                    <option>Revenda de equipamentos</option>
                    <option>Outros</option>
                </select>
            </div>

            <div class="tf-error" id="error-step-2">Preencha todos os campos antes de solicitar.</div>

            <div class="tf-footer">
                <button type="button" class="btn-tf btn-secondary" onclick="prevStep()">Voltar</button>
                <button type="button" class="btn-tf btn-primary" id="btn-submit" onclick="submitForm()">Solicitar</button>
            </div>
        </div>

    </form>

    <!-- TELA DE SUCESSO -->
    <div class="tf-success" id="tf-success">
        <div class="tf-success-icon">✓</div>
        <h3>Solicitação enviada!</h3>
        <p>Recebemos seus dados e em breve um especialista FiberWan entrará em contato com você.</p>
    </div>
</div>

<script>
    // ─── CONFIGURAÇÃO DO WEBHOOK ───────────────────────────────────────────────
    const WEBHOOK_URL = "https://hook.us1.make.celonis.com/uljmbxy88n78zvdshu2arbwc7v7homn8";
    // ──────────────────────────────────────────────────────────────────────────

    // Avança para etapa 2 com validações
    function nextStep() {
        const step1 = document.getElementById('step-1');
        const inputs = step1.querySelectorAll('input');
        const errorElement = document.getElementById('error-step-1');
        let isValid = true;

        errorElement.innerText = "Preencha todos os campos corretamente para avançar.";

        inputs.forEach(input => {
            if (!input.checkValidity()) {
                isValid = false;
            }

            if (input.id === 'tf-telefone') {
                let numbers = input.value.replace(/\D/g, '');
                if (numbers.length > 0 && numbers.length < 11) {
                    isValid = false;
                    errorElement.innerText = "Preencha o telefone completo com DDD (11 dígitos).";
                    input.style.borderColor = "var(--fw-vermelho)";
                } else {
                    input.style.borderColor = "transparent";
                }
            }
        });

        if (isValid) {
            errorElement.style.display = 'none';
            document.getElementById('step-1').classList.remove('active');
            document.getElementById('step-2').classList.add('active');
            document.getElementById('bar-2').classList.add('active');
        } else {
            errorElement.style.display = 'block';
        }
    }

    // Volta para etapa 1
    function prevStep() {
        document.getElementById('step-2').classList.remove('active');
        document.getElementById('step-1').classList.add('active');
        document.getElementById('bar-2').classList.remove('active');
    }

    // Envia o formulário via webhook
    async function submitForm() {
        const empresa = document.getElementById('tf-empresa').value.trim();
        const segmento = document.getElementById('tf-segmento').value;
        const errorElement = document.getElementById('error-step-2');

        if (!empresa || !segmento) {
            errorElement.style.display = 'block';
            return;
        }
        errorElement.style.display = 'none';

        const btnSubmit = document.getElementById('btn-submit');
        btnSubmit.disabled = true;
        btnSubmit.innerText = 'Enviando...';

        const payload = {
            nome:      document.getElementById('tf-nome').value.trim(),
            email:     document.getElementById('tf-email').value.trim(),
            telefone:  document.getElementById('tf-telefone').value.trim(),
            empresa:   empresa,
            segmento:  segmento,
            data_hora: new Date().toLocaleString('pt-BR', { timeZone: 'America/Sao_Paulo' })
        };

        try {
            const response = await fetch(WEBHOOK_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            if (response.ok) {
                window.location.href = "https://lp.fiberwan.com.br/obrigado-v2";
            } else {
                throw new Error('Resposta não OK: ' + response.status);
            }
        } catch (error) {
            console.error('Erro ao enviar:', error);
            btnSubmit.disabled = false;
            btnSubmit.innerText = 'Solicitar';
            errorElement.innerText = 'Erro ao enviar. Tente novamente em instantes.';
            errorElement.style.display = 'block';
        }
    }

    // Máscara de telefone — compatível com construtores de página
    function aplicarMascaraTelefone(el) {
        let numbers = el.value.replace(/\D/g, '').substring(0, 11);
        let v = "";
        if (numbers.length === 0)       v = "";
        else if (numbers.length <= 2)   v = numbers;
        else if (numbers.length <= 6)   v = numbers.substring(0,2) + " " + numbers.substring(2);
        else if (numbers.length <= 10)  v = numbers.substring(0,2) + " " + numbers.substring(2,6) + "-" + numbers.substring(6);
        else                            v = numbers.substring(0,2) + " " + numbers.substring(2,7) + "-" + numbers.substring(7);
        if (el.value !== v) el.value = v;
    }

    const inputTelefone = document.getElementById('tf-telefone');
    ['input', 'keyup', 'paste', 'change'].forEach(function(evt) {
        inputTelefone.addEventListener(evt, function() {
            setTimeout(function() { aplicarMascaraTelefone(inputTelefone); }, 0);
        });
    });
</script>

</body>
</html>
